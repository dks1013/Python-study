##데이터의 길이를 반환하는 len()함수
>>> len("name")
4

##거듭제곱을반환하는pow() 함수
>>> pow(2, 5)
32

##절댓값을반환하는abs() 함수
>>> A = [2, 3, 6, 7, 10]
>>> len(A)
5

##반올림한결과를반환하는round() 함수
>>> round(3.2)
3
>>> round(3.7)
4

##이진수를반환하는bin() 함수
>>> bin(24)
'0b11000'
>>> bin(3)
'0b11'

##입력값중최댓값을반환하는max() 함수
>>> max(3,2,-5,7)
7

##입력값중최솟값을반환하는min() 함수
>>> min(2,6,3,9,21,-7)
-7

##입력값을정렬한후정렬된결과값을‘리스트’로반환하는sorted() 함수
>>> sorted([3,2,6,7,1,10])
[1, 2, 3, 6, 7, 10]

##절대값
>>> abs(-5)
5
>>> abs(5)
5
